# Reversi Atelier — 立体グラフィック・滑らかな反転・戦略NPC・棋譜研究の刷新

## タイトル・保存先

**Reversi変更タイトル：feat(reversi): 立体盤面・3D反転・4段階NPCと棋譜研究を刷新**

- 対象：ゲームアプリ内のReversi（依頼中の「riversi」）。
- 保存先：[共有PR #11](https://github.com/sc8z35a-collab/smaho-gpt6/pull/11)、`genspark_ai_developer` → `main`。
- 入口：ゲーム → Reversi、または `#app=games&view=reversi`。
- 共有PRの他ゲーム・コントロールデスク等の並行更新を保持。本書はReversiの変更を説明します。
- **ユーザー指定によりテストは未実施。PR保存とマージ・本番公開は別です。**

## 1. グラフィックとモーション

- 翡翠／深藍の盤、金属調リム、くぼみ、統一した光と影。黒は黒曜石、白はパール調の両面石。
- A〜H／1〜8の座標、あなた／NPC、先後、手番、石数・配分バー、残りマス、合法手数を整理。配分バーは勝率ではありません。
- 合法手に返せる枚数を常時表示。マウス／キーボードフォーカスで返る石を強調。直前の着手は金の小さな印、ヒントは金色の候補です。
- 64セルと石のDOMを保持し、思考表示の更新で全石を作り直す挙動を廃止。
- 約400msの3D反転に、着手地点からの距離ごとに24msの差を追加。新しい石だけ約430msの着地モーション。
- 約600msの演出中は多重着手を防止。候補切替や保存再試行で反転を途中終了させないよう更新対象を分離。
- ダークモード・狭幅・キーボードフォーカスに対応するスタイル。盤面は正方形で、操作座標と表示のずれを避ける設計。
- OSの `prefers-reduced-motion` とauraの「動きを抑える」を優先し、反転・着地と演出待機を省略。
- 外部画像・追加ライブラリ・ゲーム用通信は不要。**フレームレートの実測や60fps／120fps保証はありません。**

## 2. 対局・研究機能

| 操作 | 内容 |
|---|---|
| 対局設定 | NPC／同一端末2人、4難易度、黒（先手）／白（後手）。確認後に新しい対局へ |
| 候補 ON / OFF | 合法手ガイドの切替。OFFでも合法手には着手可能 |
| ヒント | 推奨座標、反転枚数、相手の合法手数、参考手順を表示。自動着手しない |
| 分析を中止 | ヒント／局面分析中の同じボタンで探索をキャンセル |
| 待った | NPCの応手や連続パスを含め自分の着手前へ。2人戦では一手前へ |
| 思考中の待った | 実行中の探索を中止して戻す。古い結果を新しい局面へ適用しない |
| 棋譜 | 保存された手順を往復。実対局の着手とNPCを停止 |
| 手順スライダー／ボタン | 任意の保存局面へ直接移動 |
| 局面分析 | 棋譜閲覧中の盤面を探索。本対局の盤面は変更しない |
| 対局へ戻る | 最新局面へ復帰。必要ならNPCを再開 |
| 一時停止 | 着手・思考を停止。停止状態も保存し、再開ボタンで続行 |
| 盤色切替 | 翡翠／深藍へ変更・保存。対局をやり直す必要なし |
| 棋譜 TXT／JSON | 現在の対局全体を明示操作でダウンロード。自動送信なし |
| 保存を再試行 | 盤面・戦績の保存失敗時に再保存を試みる |
| 新しい対局 | 確認後に現在の盤面と棋譜を置換。累積戦績は保持 |

研究用の詳細機能は「棋譜・研究・保存」を開くと利用できます。

### ルール・戦績・操作

- 黒が先手。置けない側は自動パス。双方が置けなければ空きマスが残っていても終局し、勝敗・引き分け・石差を表示。
- NPC戦の勝敗は選んだ自分の色で判定。2人戦はNPC戦績に加算しません。
- 終局後は待った不可。棋譜閲覧と過去局面の分析は可能。
- 対局途中のヒント／局面分析／待ったは支援使用として記録。支援使用の対局もNPC戦績に含みます。終局後の研究は既存の支援フラグを変えません。
- 戦績は端末内の記録で、不正対策付きランキングではありません。
- タッチは1回で着手。PCはTabで盤へ、矢印キーで移動、Home／Endで先頭／末尾、Enter／Spaceで着手。盤端の横移動で反対の行へ飛びません。
- 読み上げ用に座標、石の色、着手可否、反転枚数、直前着手、ヒントを付与。手番・結果はライブ領域、棋譜の現在手は `aria-current` で通知。

## 3. NPCの強化

従来の固定2手先から、**時間制限付き反復深化Negamax＋αβ枝刈り＋PVS**へ変更。

| 強度 | 通常の最大深さ | 探索時間予算 |
|---|---:|---:|
| 入門 | 2手 | 約120ms |
| 標準 | 4手 | 約400ms |
| 上級 | 7手 | 約950ms |
| 達人 | 12手 | 約1,800ms |

- 浅い探索から段階的に深め、時間内に完了した最後の深さの結果を採用。
- PVSで後続候補を狭い評価窓で探索し、改善候補は必要に応じて通常の窓で再探索。
- 有望手、角、位置、killer手・history履歴による手順整列。置換表は探索ごとに最大30,000局面で、深さと評価の上限／下限／確定を区別。
- 角、位置、双方の合法手数、将来の着手機会、空きに接する石、角から続く確定辺、進行段階ごとの石差を評価。角周辺の危険度は角の占有状態に応じて変更。
- パスは探索の着手数を消費せず継続。両者パスは実石差で評価。
- 上級・達人は空き12マス以下で終局までの探索を試行。深さが空きマス数まで完了した場合だけ「終局まで読了」。常に完全読みできるわけではありません。
- ヒントは上級、達人設定では達人相当の予算。参考手順は合法手をたどった短い候補列で、最善応手列や相手の実着手を保証しません。
- 完了深さと訪問局面数を表示。「達人」は難易度名で、有段者相当・無敗を意味しません。

### 非同期・キャンセル

- 原則Web Workerで思考。非対応・CSP制限・起動失敗時は同じ探索を協調的に分割実行。
- 32ノードごとに制御を返し、約7msの区切りを目安にブラウザへ処理を戻します。低速端末での厳密な最大占有時間保証ではありません。
- Workerが無応答なら5秒でフォールバック。探索例外時は合法手の簡易候補へ退避。
- 待った・新規対局・一時停止・画面離脱・局面切替・分析中止で探索とタイマーを破棄し、Worker／Blob URLも解放。
- 非表示中は思考を中断。表示復帰時に必要なら再開。設定・確認画面を開いている間はNPC着手を保留。
- 予算は厳密な最大応答時間ではなく、端末性能・Worker起動・演出待機で変動します。**棋力・速度のベンチマークは未実施です。**

## 4. 保存・移行・書き出し

| localStorageキー | 内容 |
|---|---|
| `aura.reversiState` | version 2。盤面、手番、対戦設定、自分の色、盤色、候補、一時停止、支援使用、棋譜（最大61局面） |
| `aura.reversiStats` | 新NPC戦の勝敗・引き分け、旧WINを含むカウンタ、直近100対局の集計済みID |
| `aura.reversiWins` | 既存ゲーム一覧のWIN表示。従来値を残して新NPC勝利を加算 |

- 旧版の有効な盤面を引継ぎ。旧NPC戦は黒・標準。過去の未保存手順は復元せず、移行局面から記録。
- 盤のサイズ・値・手番を読み込み時に確認。不正な盤面は通知して新規対局へ。
- 棋譜は上限と型に加え、前局面から合法着手で再現できることを確認。不正な履歴だけなら現在の有効な盤面を残します。
- 対局IDと集計済みフラグで通常の再読込による二重計上を抑止。戦績保存が完了するまで集計完了としない設計。
- 保存失敗は共通通知と専用表示で案内し、再試行可能。旧版の終局済み対局は新NPC戦績へ再集計しません。
- 旧WIN表示と新NPC戦績の勝ち数は異なる場合があります。複数タブ同時操作・端末間同期は非対応。
- 現在の対局だけを保存。過去の複数対局の自動アーカイブはなし。保存失敗後の再読込復元は保証しません。

### ファイル出力

- TXT：日時、対戦条件、石数、支援使用、開始盤面、各着手と反転枚数・パス。
- JSON：`format: aura-reversi`、version 1。初期盤面・手番、座標・インデックス、着手列、最終盤面・石数等。
- `reversi_YYYY-MM-DD.txt`／`.json`（UTC日付）。対局途中でも出力でき、棋譜閲覧中も現在の対局全体を書き出します。
- **アプリ内再取り込みは未対応。** 移行した対局では初期盤面に移行局面が入ります。ダウンロードの扱いはブラウザ設定に依存します。

## 5. 変更ファイルと共有PR

- `js/arcade.js`：ルール・探索・Worker・対局UI・棋譜・分析・保存・書き出し。
- `css/arcade.css`：素材・3D反転・研究パネル・レスポンシブ・ダーク／動き軽減。
- `index.html`：共有Arcadeアセットのキャッシュ更新用参照。
- `README.md`：変更案内と本書へのリンク。
- `CHANGES-REVERSI.md`：本書。

最新mainと共有ブランチを取り込み、他作業を保持してReversi専用区間を統合。公開済みの共有履歴を壊さないため、他作業を含む強制squash／強制pushは行いません。

## 6. テスト未実施

依頼に従い、このReversi作業では**自動テスト、単体・回帰テスト、対戦シミュレーション、構文チェック、lint、ビルド、ブラウザ起動、スクリーンショット、実プレイ、性能測定を実行していません**。

実施した確認はソースの読み合わせ、Git差分・競合、コミット・push・PR保存状態です。既存PRにある別作業の過去のテスト結果は、このReversiの動作保証ではありません。本書の「対応」は実装内容であり、実機検証済みを意味しません。
