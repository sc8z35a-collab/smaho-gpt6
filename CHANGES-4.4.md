# auraOS 4.4 — 既存アプリの30件の追加・改善

基点：`52d0122`（4.3.1）。既存の30アプリを維持し、5アプリに各6件を追加・改善しています。

## 連絡先

1. **グループ分類**：家族・友人・仕事など任意のグループを付け、一覧を絞り込み。
2. **お気に入りだけ表示**：既存のお気に入り登録から、必要な相手だけ表示。
3. **並べ替え**：お気に入り優先／名前順を選択。
4. **重複候補**：同じ電話番号・メールを持つ連絡先を表示。自動統合・削除はしません。
5. **1件のコピー**：名前・電話・メール・グループ・メモをまとめてコピー。クリップボードが使えない環境では選択可能なテキストを表示。
6. **絞り込み結果の書き出し**：表示中の連絡先だけをvCardに書き出し。既存の全件出力も維持。

## 単位換算

7. **時間**：秒・分・時間・日・週を換算。
8. **エネルギー**：J・kJ・cal・kcal・Wh・kWhを換算。calは熱化学カロリー。
9. **圧力**：Pa・hPa・kPa・bar・atm・psiを換算。
10. **よく使う換算**：数値と単位の組み合わせを最大20件保存して再利用。
11. **履歴**：「履歴に残す」で最大30件を保存。呼び出し・個別削除に対応。
12. **表示精度**：有効数字4・8・12桁を選択して保存。空欄・無限大・絶対零度未満は結果を表示しません。

## リマインダー

13. **検索**：タスク名・メモ・手順を横断検索。
14. **並べ替え**：優先度順・期限順・名前順。完了済みは後ろに表示。
15. **タスクのメモ**：タスクごとの詳細説明を保存。
16. **手順チェック**：1タスクに最大30手順、各160文字まで。進捗を一覧に表示し、手順ごとにチェック。タスク全体の完了とは別管理。
17. **複製**：タスク・メモ・手順をコピーし、未完了から使い直す。
18. **期限の延期**：詳細画面から1日延期。期限なし・期限超過の場合は明日に設定。

## 今日

19. **日付切替**：前後の日・日付指定・今日に戻る。予定・集中時間・習慣の記録を選択日に合わせて表示。
20. **7日間の予定**：選択日から7日間の予定数と予定の入口を折りたたみ表示。
21. **買い物の残り**：全リストの未購入品数と予定額をまとめ、買い物へ移動。
22. **読書の続き**：読書中の本を最大3冊表示し、残りページと詳細への入口を追加。
23. **この日のひとこと**：日ごとに160文字まで保存。空欄で保存すると削除。
24. **この日のまとめの出力**：予定・ひとこと・未完了タスクをテキスト保存。

過去日のタスク表示は当時の履歴の再現ではありません。現在の未完了タスクから、選択日までの期限と期限なしを表示します。買い物・読書のカードも現在の状態であることを明記します。

## 買い物

25. **リスト内検索**：品名で絞り込み。全角・半角を正規化。
26. **並べ替え**：分類ごとに未購入優先・名前順・合計金額の高い順を選択。
27. **リスト別予算**：全品購入後の予算残額・超過額を表示。0円で解除。
28. **購入済み金額**：未購入予定額・購入済み金額・全品合計を分けて表示。価格未入力は0円として扱うことを明記。
29. **定番リスト**：内容を最大20件・各100品までテンプレート保存して再利用。同名の未購入品は追加せず、購入済み品は新たな未購入品として追加。
30. **削除を元に戻す**：品の削除と購入済み一括削除を、直前の1操作に限り取り消し。取り消し履歴はページを開いている間のみ保持。

## 画面と保存

- 検索・追加・チェックを主な操作にし、書き出し・定番リスト・管理などは折りたたみへ整理。
- 既存の配色を使い、ダークモード・狭い画面向けのスタイルとキーボードのフォーカス表示を追加。
- ホーム検索、集中タスク選択、「今日」、書き出しは既存の共有データを使用。
- 新しい保存キー：`dailyIntentions`、`shoppingBudgets`、`shoppingTemplates`、`conversionPresets`、`conversionHistory`、`conversionPrecision`（すべて`aura.`接頭辞）。
- 既存の`contacts`に`group`、`reminders`に`note`と`steps`を追加。従来レコードは未分類・メモなし・手順なしとして扱う。
- 自動通信・外部送信は追加していません。保存先は従来と同じブラウザ内です。
- 拡張スクリプトは`everyday.js`と`productivity.js`の後、初回起動ルートを処理する`games.js`の前に読み込みます。

## 実施範囲

依頼により、自動テスト・手動動作テスト・ブラウザ表示テスト・性能計測は実施していません。コードの読み取りと差分の確認のみであり、動作確認済みという意味ではありません。

## 続けて実施した既存機能の改善・最適化・修正

- **関連データの保存失敗**：複数キーを変更する前に元の内容を保持し、途中失敗時には復元を試行。メモのごみ箱移動・復元、メモ/ファイルのフォルダ削除、ファイルと変更履歴、読書の進捗と履歴・引用、買い物リスト削除に適用。成功してから画面上のモデルを更新します。localStorage自体の障害や他タブの変更まで含む完全なトランザクション保証ではありません。復元に失敗した場合も表示します。
- **ファイル検索・並べ替え**：一覧を開いた時にUTF-8容量と検索用文字列を一度計算し、並べ替えの比較や検索入力で再利用。全角・半角を正規化。実測の速度改善率は未測定です。
- **長文の一覧表示**：メモ・日記のカード本文は先頭320文字まで描画。本文の保存・編集・検索は全文のままです。
- **スケッチの描画処理**：入力点は従来の上限内で保持し、再描画を画面更新タイミングにまとめる。終了・画面移動時には保留描画を確定し、予約処理を解除。
- **スケッチの複数指入力**：描いている指と異なる指の終了イベントで線を確定しないよう修正。指を離した位置も記録。
- **本文上限**：メモ・日記の箇条書きなどの挿入ボタンから文字数上限を超えないよう修正。
- **ファイル読み込み**：20件超の未読込件数を表示。形式・容量・80文字の名前制限と、デコード後のUTF-8容量を確認。
- **ファイル名**：入力前後の空白を除去して拡張子を判断。複製時は拡張子を保って80文字以内に収める。
- **読書記録の出力**：メモ・引用に加えて、各回の読書ページ・時間と合計時間を含める。
