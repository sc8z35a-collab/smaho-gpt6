# Crystal Field — 高精細グラフィック・滑らかなモーション・探索機能のアップグレード

## 変更タイトル / PR掲載用タイトル

**feat(crystal): クリスタルフィールドを高精細化し、滑らかな演出・日替わり探索・操作支援を追加**

- 保存先：共有開発ブランチ `genspark_ai_developer`
- PR：<https://github.com/sc8z35a-collab/smaho-gpt6/pull/11>
- 対象：ゲームアプリ内の **Crystal Field（クリスタルフィールド）**
- 起動経路：ゲーム → Crystal Field、または `index.html#app=games&view=mines`
- **利用者の指定により、今回の変更についてテストは一切実行していません。**
- 本書は実装内容の説明です。性能測定結果・動作保証・本番反映完了の報告ではありません。

## 1. グラフィックの改善

| 項目 | 実装内容 |
| --- | --- |
| 結晶アート | 多面体の面ごとに色を分けたSVG。エッジの反射、影、透明感のある明暗を表現 |
| ゲーム一覧 | Crystal Fieldのカードとおすすめ表示で使う結晶アートも更新 |
| 盤面 | 深い青緑のフレーム、ガラス調の未開封タイル、沈み込んだ開封済みマス |
| 旗・結果 | ベクター旗、接触した鉱石の強調、誤った旗に「×」、探索レポート |
| 結晶テーマ | アクア → アメジスト → アンバーの3種類。盤面・見出し・結晶の色を切替 |
| 見やすさ | 1〜8の数字に色を設定。テーマを変えても数字色・旗・誤旗の意味は維持 |
| ダークモード | 情報パネル・ボタン・本文をauraのダークモードに合わせる |

追加の画像ダウンロードや画像生成サービス、外部ライブラリは使用しません。SVGとCSSによる立体的な表現であり、WebGLの3Dゲームへの変更ではありません。

## 2. 滑らかなモーションと描画の軽量化

- **波状の開封演出**：開いた位置からの距離に応じて最大280msずらし、420msの拡縮・フェードを適用します。
- **旗の出現演出**：250msの小さなポップイン。
- **浮遊する結晶**：見出しの結晶にゆっくりした上下動と回転。
- **クリア演出**：6個の粒子を有限時間だけ表示。探索レポートをフェード表示。
- **既存DOMを再利用**：操作ごとに盤面全体をHTMLで作り直さず、変更されたマスの内容を更新。キーボードフォーカスも保持します。
- **一括処理**：連鎖開封と数字からの周辺開封を1操作分まとめて処理し、保存・描画を集約します。
- **寸法の安定化**：マス内のSVG・数字を絶対配置し、旗や結晶が増えてもセル寸法を変えない構成にしています。
- **軽量描画**：画面下の描画ボタンで影・フィルター・アニメーションを減らします。ゲームルールは変えません。
- **動きを減らす設定**：OSの `prefers-reduced-motion` とauraの設定を尊重し、演出を停止します。
- アプリ終了時にタイマー、イベントリスナー、長押し待機、オーバーレイ監視、音声リソースを解放します。

**60fps / 120fpsなどの固定フレームレートは保証しません。** 今回はテスト・実機確認・FPS測定を行っていないため、滑らかさや負荷は端末とブラウザに依存します。

## 3. ゲーム性の改善

### 既存ルールを維持

| 難易度 | 盤面 | 鉱石 |
| --- | --- | --- |
| 初級 | 8 × 8 | 10個 |
| 中級 | 10 × 10 | 18個 |
| 上級 | 12 × 12 | 28個 |

最初に開くマスと周囲8マスは安全です。数字は周囲の鉱石数を表します。鉱石以外のマスをすべて開くとクリアです。ゼロの周囲は連鎖して開きます。

旗の数が数字と一致している場合、開封済みの数字を押すと未開封の周囲をまとめて開きます。**旗が間違っていると鉱石を開いて終了することがあります。** 旗自体の正誤を途中で自動判定する機能ではありません。

### 新しい探索支援

- **安全スキャン**：1探索につき3回。ボタンを押すと安全な未開封マスを1つ開きます。ゼロなら連鎖開封します。
- スキャンは旗のない安全マスだけを対象にします。対象がなければ消費せず、旗を見直す案内を表示します。
- スキャンを使用したクリアは **アシスト最短** に記録し、通常の最短記録を更新しません。
- 開封数・総安全マス数・達成率を進捗バーに表示します。
- 開いた数字へのマウスオーバー／キーボードフォーカスで周囲8マスを強調します。
- 旗の追加は鉱石数まで。旧セーブで上限以上の旗があっても、既存の旗を外せます。

このパズルは「すべて論理だけで解ける」盤面生成ではありません。推測が必要な配置もあります。安全スキャンは論理解説ではなく、ゲーム側が安全を確認したマスを開くアシストです。

### 日替わりチャレンジ「今日の鉱脈」

1. 「今日の鉱脈」を押して、現在の盤面の置き換えを確認します。
2. **中央の＋マス**から開始します。初手前の旗立ては行えません。
3. 端末のローカル日付と難易度をシードに、同じ開始位置から同じ盤面を生成します。
4. 同じ日の再挑戦は同じ盤面。日替わり中の「新しい盤面」は、その日の鉱脈のやり直しです。
5. 日付をまたいでも進行中の盤面は変えません。「今日の鉱脈」を押し直すと、現在の日付の盤面になります。
6. フリー探索とは別に、日付・難易度ごとのノーヒント最短／アシスト最短を保存します。

日替わりのクリアはフリー探索の累計勝利数・通常最短記録に加算しません。同じ日・難易度の再クリアは1件にまとめ、最短だけを更新します。日替わり記録は最近更新した90件まで保持します。オンラインランキング、時刻のサーバー検証、端末間の成績同期はありません。

## 4. 操作方法と便利機能

| 操作 | スマートフォン / マウス | キーボード |
| --- | --- | --- |
| マスを開く | 探索モードでタップ／クリック | フォーカスしたマスでEnter / Space |
| 旗を切替 | 旗モードでタップ、約440msの長押し、右クリック | F（マスにフォーカス中） |
| 選ぶマスを移動 | マスを選ぶ | 矢印キー。盤面の端で停止 |
| 一時停止 / 再開 | 一時停止・再開ボタン | P |
| 安全スキャン | 安全スキャンボタン | Tabでボタンへ移動しEnter / Space |
| 盤面の拡大 | 「盤面を拡大」。拡大時は横・縦スクロール | 矢印で盤面内を移動 |
| 描画負荷の切替 | 「高精細描画」 / 「軽量描画」 | ボタンを選択して操作 |
| テーマの切替 | 「結晶色」ボタンを押すたびに3色を循環 | 同左 |
| 効果音 | 「効果音 OFF」 / 「効果音 ON」 | 同左 |
| 成績 | 「成績」で最短・日替わり・最近の探索を表示 | 同左 |

長押し時は指の移動、スクロール、キャンセルを監視し、長押し直後のクリックによる誤開封を抑止します。ブラウザごとのジェスチャー挙動は未検証です。長押しが使いづらい端末では旗モードを利用してください。

拡大表示ではセルを44px以上に広げます。通常表示の上級盤面は狭い端末では小さくなるため、拡大表示を推奨します。

### 一時停止・タイマー

- 初手を開くまでは時間を計測しません。
- プレイ中は `performance.now()` の差分を積算し、約250msごとに表示を更新します。
- 一時停止中は盤面を隠し、操作を無効にします。
- タブ非表示、ウィンドウのフォーカス喪失、オーバーレイ表示、アプリ移動時に一時停止します。
- **戻ったときは手動で再開**します。再読込から復元した進行中の盤面も一時停止で開きます。
- 2秒を超える更新間隔は端末停止等とみなし、加算せず一時停止します。競技用の厳密な計時ではありません。
- 終了時間は秒単位で切り上げ、最小1秒。結果画面と記録で同じ数値を使います。旧最短記録はそのまま残します。

### 効果音

初期状態はOFFです。ON時はWeb Audioで開封・旗・スキャン・クリア・失敗の短い音を合成します。auraのサウンド設定と音量に従います。音声ファイルのダウンロード・外部生成・録音は行いません。

音声非対応、音声の自動再生制限、ミュート時でもパズルは継続できます。音声初期化直後の操作では音が出ない場合があります。効果音OFF・プレイ中の一時停止・アプリ終了時に音声リソースを閉じます。

## 5. 保存仕様と既存データの扱い

すべて同じブラウザのlocalStorageに保存し、キーには `aura.` が付きます。

| キー（接頭辞を省略） | 内容 |
| --- | --- |
| `minesState` | バージョン3。難易度、盤面、旗、開封、経過時間、手数、スキャン使用数、日替わりの日付、一時停止状態 |
| `minesWins` | 従来からのフリー探索の累計勝利数。アシスト使用のフリークリアも含む |
| `minesBest-easy/normal/hard` | 難易度別のフリー・ノーヒント最短。既存値を引き継ぐ |
| `minesAssistedBest-easy/normal/hard` | 難易度別のフリー・アシスト最短 |
| `minesHistory` | 最近終了した探索30件。フリー／日替わり、結果、時間、手数、ヒント数、終了日時 |
| `minesDailyRecords` | 日付・難易度別の日替わりクリア記録90件。通常／アシストを分離 |
| `minesPreferences` | 軽量描画、拡大表示、結晶テーマ、効果音の選択 |

- 盤面操作時・約5秒ごと・一時停止・画面離脱時に進行を保存します。
- 旧形式のセーブは配列・範囲・重複・盤面サイズ・勝敗整合性を確認して読み込みます。旧経過秒数はミリ秒へ変換します。
- 不正なセーブは新しい初級盤面に切り替えます。他アプリのデータ、累計勝利数、既存最短記録は削除しません。
- 途中で作り直した探索は終了履歴に含めません。過去の未保存履歴を後から生成しません。
- 勝利済みの盤面を読み込むだけでは勝利数を再加算しません。
- データ保存容量が不足した場合は既存の共通保存エラーを表示します。OSによる強制終了時の直前の時間、複数タブからの同時編集、保存失敗時の完全な復元は保証しません。
- プライベートブラウズやブラウザデータ削除では記録が失われる場合があります。クラウド保存ではありません。

## 6. 変更ファイル

- `js/arcade.js`：Crystal Fieldのゲームロジック・描画更新・操作・保存・日替わり生成・効果音・記録画面。
- `css/arcade.css`：結晶・盤面・モーション・3テーマ・拡大・軽量・ダーク・動き軽減。
- `index.html`：`arcade.js` / `arcade.css` のキャッシュ更新識別子。
- `README.md`：本書への案内。
- `CHANGES-CRYSTAL-FIELD.md`：本書。変更タイトル、説明、操作、制限、テスト未実施を記載。

共有PRにはコントロールセンターや他ゲームなどの既存・同時進行の変更も含まれます。それらを上書きせず同期して保存しています。本書はCrystal Fieldの変更のみを説明します。

## 7. テスト・公開状態

**依頼に従い、今回のCrystal Field作業では自動テスト、回帰テスト、ブラウザ操作、実機動作検証、構文チェッカー、性能測定を実施していません。** コードの読解とGit／PRの保存状況の確認のみを行っています。

共有PRに記載された他機能のテスト結果は、この変更の検証結果ではありません。長押し、各画面幅、ダークモード、音声制約、連続操作、セーブ移行、FPSは未検証です。

コードと本書をPRへ保存していますが、**mainへのマージ・GitHub Pagesへの本番反映はこの作業では行いません。**
