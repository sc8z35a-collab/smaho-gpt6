# Orbit Breaker — Flight Upgrade 完全版

## PRタイトル

**`feat(orbit): 高精細グラフィック・なめらかモーション・再開保存とミッションを追加`**

- 対象：ゲームアプリの **Orbit Breaker**（ブロック崩し、内部ID `breaker`）。
- 保存先：[共有PR #11](https://github.com/sc8z35a-collab/smaho-gpt6/pull/11)
- ブランチ：`genspark_ai_developer` → `main`
- 上記はOrbit変更分のタイトルです。共有PRのタイトルには既存機能名を残してOrbit強化を追記します。
- 初回の描画刷新と、続行依頼による保存・ミッション・操作性の拡張をまとめています。「完全版」は今回の依頼範囲の実装を指し、テスト済み・不具合なしを意味しません。

> **利用者の指定により、今回のOrbit変更はテスト未実行です。** 自動テスト・回帰テスト・構文チェック・ブラウザ起動・手動プレイ・実機確認・性能測定は行っていません。ソースの読み取りとGit/PR保存状態の確認のみです。120Hzは物理演算の固定刻みであり、120fpsを実測・保証したものではありません。

## 1. 高精細グラフィックとモーション

- 星雲、陰影付き惑星、軌道リング、遠近感のある床面をCanvasで生成。星は奥行きごとに緩やかに移動します。
- ブロックに面取り・反射・影・耐久マーク・ひび割れ。ノヴァブロックには `N` と残り耐久を表示。
- 発光する球体、連続した軌跡、破片・波紋、金属調パドルを追加。
- スコア・残機・セクター・コンボ・進捗・強化状態を宇宙船風UIに整理。開始・停止・結果はHTMLの操作パネルを使用。
- 高画質は表示サイズに合わせたHiDPI（DPR最大2）、軽量はDPR 1。軽量では星・破片・発光を削減。
- 物理計算は1/120秒固定、描画はrequestAnimationFrame。ボール・パドル・アイテムを前後位置から補間します。
- パドルは時間に基づく追従。衝突は移動線分と半径分広げた矩形の最初の接点を使い、壁・ブロック・パドル・シールドを処理します。
- 背景・ブロック素材はキャッシュ。破片は高画質150個／軽量36個、波紋8個、落下アイテム6個、軌跡18点まで。
- 1ステップ内の衝突処理は最大6回、1描画の物理更新は最大9回。250ms超のフレーム中断は安全のため停止します。
- HUDは通常約100msごとに更新。停止中は常時ループを止め、退出時にイベント・監視・フレーム要求・音声コンテキストを解放。
- 外部画像・動画・音源・ライブラリの追加取得は不要です。短い効果音もプログラムで生成します。

## 2. ゲーム性の拡張

| 難易度 | 初期残機 | パドル幅（論理座標） | 初期速度 | 得点係数 |
|---|---:|---:|---:|---:|
| リラックス | 5 | 104 | 235 | 1倍 |
| スタンダード | 3 | 84 | 290 | 1.5倍 |
| エキスパート | 3 | 70 | 345 | 2倍 |

速度はセクターごとに13増え、上限475。難易度変更は確認後に新規フライトへ切り替えます。

- 星座・ダイヤ・ゲート・波・クロス・要塞の6種の配置が巡回。最大7行、最大耐久3で、破壊不能ブロックはありません。
- パドルに戻るまでの連続ヒット5回ごとに倍率上昇、最大5倍。基本得点は耐久ヒット8点／破壊20点に倍率と難易度係数を掛けます。
- `N` を直接破壊すると近隣へ1ダメージ。無制限の連鎖再帰は行いません。
- セクタークリア：`150 × セクター × 難易度係数`。そのセクターで落球がなければ、さらに `200 × セクター × 難易度係数`。
- 3セクタークリアごとに残機+1（最大7）。クリア後は発射待ちになり、準備して次へ進めます。

### 強化アイテムとFOCUS

7個破壊ごとに `W → T → S` の順で落下。パドルで回収します。同時落下は最大6個です。

| 表示 | 効果 |
|---|---|
| W | パドル幅+32、14秒。再取得は残り時間を14秒に更新 |
| T | ボール速度を64%に、9秒。再取得は残り時間を9秒に更新 |
| S | 落球を1回防止。最大2回分保持 |
| FOCUS | チャージ100%を消費し、ボール速度を64%に、4秒 |

ヒットとミッションでチャージが増えます。TとFOCUSの減速は掛け合わせません。強化タイマーは飛行中だけ進み、停止・発射待ち・再開カウントダウン中は減りません。落球でW/T/Sと発動中のFOCUSは終了し、チャージは保持します。

### セクターミッション

| 目標 | 基本報酬 |
|---|---|
| ブロックに12回ヒット | 150点 + FOCUS 20% |
| 8回連続ヒット | 250点 + FOCUS 20% |
| アイテムを2個回収 | 200点 + FOCUS 20% |

得点報酬に難易度係数を掛けます。報酬は各セクター1回、次のセクターで目標更新。達成・受領状況も保存し、ノヴァの周辺ダメージもヒットに含めます。結果画面にはミッション達成数・ノーミスクリア数も表示します。

## 3. 操作方法

入口：ゲーム一覧 → **Orbit Breaker**。起動ハッシュは `#app=games&view=breaker`（PRのコードを配信する環境で有効）。

| 操作 | 内容 |
|---|---|
| 開始／再開 | 開始・再開。飛行中の再開には3秒カウントダウン |
| 発射／Space／開始後の盤面タップ | ボール発射。ボタンにフォーカスがある場合のSpaceは通常のボタン操作を優先 |
| ドラッグ／マウス移動／← →／画面内左右ボタン | パドル移動。左右ボタンは押している間移動 |
| 発射角スライダー | 発射前に-55〜+55度を調整 |
| 発射ガイド | 壁反射込み最大3区間・総距離580の軌道。ブロックに当たればそこで停止 |
| P／一時停止 | 停止・再開 |
| F／FOCUS | チャージを消費してスロー発動 |
| 盤面重視／通常表示 | 設定や装飾を畳む／戻す。切替時は安全のため停止 |
| 保存して停止 | 停止して進行と記録を保存 |
| 音 ON/OFF | 初期OFF。auraのサウンド・音量設定にも従う |
| リセット | 確認して同じ難易度の新規フライトへ。旧ベストは保持 |

盤面重視はブラウザの全画面機能ではありません。短い画面・横向きでは必要に応じてスクロールできます。通常表示へ戻せば難易度・画質・音・ミッション・保存説明が再表示されます。

## 4. 保存と再開

| localStorageキー | 内容 |
|---|---|
| `aura.breakerBest` | 旧記録を保持。ゲーム一覧では全難易度共通の最高点 |
| `aura.breakerPreferences` | 難易度・画質・効果音・盤面重視表示 |
| `aura.breakerRecords` | 難易度別の最高点・到達セクター・最大連続ヒット |
| `aura.breakerState` | バージョン付き進行1枠。球・パドル・耐久・残機・得点・強化・アイテム・ミッション・時間等 |

- プレイ中の約10秒ごと、停止、落球、クリア、ゲーム終了、アプリ退出、ページ非表示・離脱時に保存します。
- 保存前の直近進行は強制終了等で失われる可能性があり、終了イベントの実行は保証しません。
- 進行と記録は `Aura.saveBatch` でまとめて保存。失敗時はエラーと保存状態を表示し、成功と偽りません。localStorageには本当のトランザクションがなく、ロールバックはベストエフォートです。
- 復元時に形式・難易度・数値範囲・配列長・耐久・速度・アイテム種別を確認。描画キャッシュ・破片は再生成します。
- 復元直後は必ず停止状態。離れていた実時間でボールや強化時間を進めず、再開操作を待ちます。
- 不正・非対応データは新規開始へ戻します。旧最高点は削除しません。
- 新規開始・リセット・難易度変更は保存枠を置き換えます。履歴スロット・クラウド同期はありません。
- 複数タブの進行は最後の保存が優先。最高記録は書込直前の保存値と最大値を取りますが、同時書込みの完全な競合制御はなく、1タブでの利用を推奨します。
- ブラウザの保存データ削除・aura全データ削除で内容は失われます。

## 5. 中断とアクセシビリティ

- 非表示タブ、フォーカス喪失、ヘルプ等のオーバーレイ、アプリ退出で停止し、自動再開しません。
- 停止時にキー入力・ポインタ捕捉を解除し、移動入力が残ることを防ぎます。
- OSまたはauraの「動きを減らす」で背景移動・軌跡・破片・波紋・ヒットの点滅を抑制。必要な球・パドルの動きは残します。
- フォーカス表示、ラベル付きスライダーと進捗、重要状態の読み上げ領域を追加。
- Canvas中心のリアルタイムゲームであり、視覚を用いず盤面全体を把握する操作体験までは提供しません。

### 完全版の最終仕上げ

- 端末回転・ウィンドウ変更などで盤面表示サイズが変わった場合も一時停止し、再開操作を待ちます。
- 開始・再開時はスクロール位置を変えず盤面へフォーカスし、Spaceによる発射を続けやすくしました。
- 左右操作ボタンのポインタも停止時に解放し、複数指による入力の取り違えを抑制します。
- 自動保存・手動保存には画質・音等の設定も含め、以前の設定保存に失敗した場合も再試行します。
- 「保存して停止」の重複書込みを抑えました。これらもソース上の実装であり、テスト未実行です。

## 6. 変更ファイル

- `js/arcade.js`：Orbitの描画・物理・ルール・操作・保存・ミッション。
- `css/arcade.css`：Orbit専用UI・盤面重視・カウントダウン・ミッション。
- `index.html`：既存の共有アセット参照を保持し、Orbit用キャッシュキーを追加。
- `README.md`：本書への案内と、旧保存仕様の説明更新。
- `CHANGES-ORBIT.md`：タイトル・仕様・操作・保存・制限をまとめた本書。

## 7. 未検証と公開範囲

**今回のOrbit変更はすべて未テストです。** 同じPRや過去のREADMEにある別機能のテスト結果を、Orbitの確認結果として扱わないでください。

未確認：実機速度、高DPR、狭い画面、タッチ／ペン、高リフレッシュレート、衝突境界、保存復元、容量不足、複数タブ、音声制限、動き軽減、他ゲームとの回帰。性能・FPS・電池消費は未測定です。

共有ブランチの並行変更は取得・統合して保持します。既存作業を消す共有履歴の強制書換えは行いません。Orbitの作業として `tests/` の変更や実行は行っていません。

**今回の保存先はPRです。マージ・本番公開・GitHub Pagesのデプロイ確認は行っておらず、本番サイトに直ちに反映されるものではありません。**
