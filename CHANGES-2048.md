# 2048 Studio — 高品質グラフィック・なめらかモーション・プレイ支援の強化

## PRタイトルと保存先

**2048改善のタイトル：`feat(2048): 立体描画・なめらかモーション・履歴保存と局面研究を強化`**

- 保存先：[共有PR #11](https://github.com/sc8z35a-collab/smaho-gpt6/pull/11)
- ブランチ：`genspark_ai_developer` → `main`
- 既存PRのコントロールデスクや他ゲームの並行変更は保持しています。本書は2048の変更内容です。
- 共有PRの全体タイトルには複数機能を含め、本文に2048のタイトルと本書へのリンクを記載します。

> **依頼によりテストは実行していません。** 前回実装と今回の追加改良ともに、構文チェック、ブラウザ起動、手動プレイ、自動テスト、回帰テスト、ベンチマークは未実施です。本書は実装仕様であり、動作確認済みという意味ではありません。共有PRの過去の別機能のテスト結果は、今回の2048の検証結果ではありません。

## 何が変わったか

4×4の基本ルールはそのままに、グラフィック・描画方式・操作・保存機能を刷新しました。

| 分野 | 実装内容 |
|---|---|
| 高品質グラフィック | 陶器 / オーロラの2素材、ハイライト・面取り・接地影、くぼみのある盤面、数字ごとの色、2048の金色表現 |
| モーション | 実際の移動経路に沿うスライド、合体時の弾み、新タイルの出現、加点の浮遊表示 |
| 追加演出 | 合体地点に光の輪と4つの光粒。常時動く描画ループは追加せず、次の移動で古い演出を解除 |
| 取り消し・やり直し | 合計32手分の履歴を端末に自動保存。再読み込み後も復元 |
| 局面保存 | 好きな局面を1枠保存。ミニ盤面・得点・手数・保存日時を表示し、後で復帰 |
| 局面の比較 | 4方向の加点・合体数・新タイル出現後の空きマス数を比較する表 |
| 先読みヒント | 新タイルの出現と次の一手を評価し、推奨方向を文章とボタンで案内 |
| 目標・記録 | 次の目標バー、手数、空きマス、最大タイル、連続合体、自己記録 |
| 続行・終了 | 2048達成後の続行、ゲームオーバーからの取り消し、確認付き再スタート |
| 操作 | スワイプ・方向ボタン・矢印キー・WASD、Zで戻す、Y / Shift+Zでやり直し |

## グラフィックとモーションの仕組み

- **背景の16マスと動くタイルを分離**し、毎手の盤面全体のHTML再生成を廃止。移動するタイルDOMを保持します。
- ルール計算で移動元・移動先・合体地点を記録し、Web Animations APIの`translate3d`で補間します。
- 親要素は移動、子要素は拡大・縮小を担当。移動と合体演出が干渉しにくい構成です。
- 「なめらか」は150ms、「きびきび」は95msの移動時間。合体後に220msの弾み、新タイルには180msの出現演出を設定しています。
- 光の輪は420ms、光粒は360ms。1手につき最大8合体地点×5演出要素に制限し、次の手や画面終了時に破棄します。演出の計算はゲーム用の乱数を消費しません。
- 得点・盤面の論理状態はモーション開始前に保存するため、移動中に画面を離れても、その一手の結果が保存対象になります。
- 先行入力は最新の1方向のみ保持。キーリピートが待ち行列に蓄積しないよう制御しています。
- タブ非表示・フォーカス喪失時には先行入力を破棄して描画を確定。ゲーム終了時にはタイマー・イベント・監視・アニメーションを解除します。
- auraの「動きを抑える」とOSの`prefers-reduced-motion`を優先。Web Animations API非対応の場合も即時表示へ切り替えます。
- ダークモード、狭い画面、桁数に応じた文字サイズ、フォーカス表示を実装しています。
- 画像や描画ライブラリの追加ダウンロードはありません。既存の共通フォント参照は残ります。

**60fps・120fpsなどの実測値や滑らかさを保証するものではありません。端末・ブラウザでの表示と速度は未検証です。**

## プレイ支援の詳細

### 取り消しとやり直し

盤面・得点・手数・連続合体・達成後の続行フラグ・乱数状態を一緒に復元します。戻した後に同じ方向へ動かしても、新タイルを引き直して有利な結果だけを選ぶことはできません。

- 履歴は取り消しとやり直しを合わせて最大32状態。盤面と同じ保存データに含めます。
- 戻った後に別の有効な手を進めると、やり直し履歴は消えます。
- 新規ゲームでは履歴を消去。アプリ切替や再読み込みでは保存された履歴を復元します。
- 最高点と自己記録は取り消しても減りません。
- 旧版に存在しなかった履歴を過去にさかのぼって復元することはできません。

### 局面を保存する

「一手の予測と局面の保存」を開き、「局面を保存」を押します。

- 保存は1枠。すでに保存がある場合は、上書き前に確認します。
- 保存した盤面のミニ表示と得点・手数・日時で内容を識別できます。
- 「局面に戻る」は確認後に盤面・得点・手数・続行状態・乱数状態を復元します。
- 復帰する直前の状態を取り消し履歴に入れるため、復帰自体も「戻す」で取り消せます。復帰前のやり直し履歴は消えます。
- 新しいゲームを始めても保存局面は残ります。
- 「保存を削除」は確認付き。現在のゲームと取り消し履歴には影響しません。
- 保存・削除に失敗したときは成功と表示せず、以前の保存局面をメモリ内でも維持します。

### 方向別の予測とヒントの違い

**方向別の予測表**は、その方向に動かしたときの合体得点・合体組数・新タイル1枚出現後の空きマス数を、現在の盤面から計算します。移動できない方向も明示します。新タイルの位置や数字を予告するものではありません。

**ヒント**は、各合法手について空きマスへの2/4の出現を90%/10%で重み付けし、そこからの次の合法手を評価します。空きマス、数字の並び、隣接する数字の差、大きい数字を角に置けるかを考慮します。推奨方向はボタンと比較表で強調します。

いずれも盤面・乱数・得点・履歴を変更しません。ヒントは目安であり、最適解・勝利確率・勝利を保証するものではありません。

### 基本ルールは維持

- 同じ数字のタイルを合体し、1手で同じタイルが繰り返し合体しない標準ルールです。
- 合体した数字を得点に加算。連続合体の表示には追加得点や得点倍率はありません。
- 無効な方向への入力では得点・手数・乱数・履歴を変更しません。
- 2048に到達すると「続ける」まで移動を停止し、その後も4096以上を目指せます。
- 目標バーは「最大タイル / 次の目標」の数値比。勝利確率や必要手数の推定ではありません。

## 保存データ・互換性

保存先はブラウザのlocalStorageです。外部送信・クラウド同期・オンラインランキングはありません。

| キー | 内容 |
|---|---|
| `aura.2048state` | 既存の16マス・得点を維持。手数、連続合体、続行フラグ、乱数、取り消し・やり直し履歴を保存 |
| `aura.2048best` | 従来の最高点を引き継ぎ |
| `aura.2048records` | 自己最大タイル・最多連続合体 |
| `aura.2048preferences` | 陶器 / オーロラ、なめらか / きびきび |
| `aura.2048checkpoint` | 保存局面1枠と保存日時。削除時は`null`を保存 |

- 旧形式の`{tiles, score}`も読み込み、新項目は初期値で補完します。旧形式ですでに2048以上に到達していた場合は続行状態として引き継ぎます。
- 盤面は16要素、0または2のべき乗、得点は非負の安全な整数であることを確認。不正な盤面は新規盤面に戻し、その旨を表示します。
- 履歴と保存局面は許可したフィールドだけを読み込みます。履歴の中に履歴を入れる構造は保存しません。不正な履歴・保存局面は復元対象から除外します。
- 盤面・最高点・自己記録は既存の`Aura.saveBatch`でまとめて保存。基盤のロールバックはベストエフォートです。
- 保存失敗時は「未保存・保存し直す」を表示。画面下のボタンで再試行できます。失敗したまま閉じるとメモリ内の進行は失われます。
- 複数タブ間の排他制御はありません。同時プレイでは後から保存したタブが保存内容を更新します。

## 操作とアクセシビリティ

| 操作 | 方法 |
|---|---|
| 移動 | スワイプ / 矢印キー / WASD / 方向ボタン |
| 取り消し | 戻す / Z / CtrlまたはCmd+Z |
| やり直し | やり直す / Y / Shift+Z / CtrlまたはCmd+Shift+Z |
| ヒント | ヒントボタン |
| 予測・局面保存 | 一手の予測と局面の保存 |
| 素材・速さ・記録 | 見た目とプレイ記録 |
| 保存再試行 | 画面下の保存状態ボタン |

日本語の操作ラベル、盤面の行別読み上げラベル、得点・結果のライブ通知を追加しました。入力欄、編集中、日本語変換中、モーダル表示中のゲームキー入力を抑制します。主ポインターのみ受け付け、右クリック・複数指・キャンセルされたスワイプを誤操作として処理しない構成です。

## ファイルと未検証範囲

- `js/games.js`：2048のルール・描画・入力・ヒント・予測・履歴・保存・ヘルプ。既存の`Aura.gameMath.mergeLine`インターフェースは維持。
- `css/arcade.css`：`.studio-2048` / `.g2048-*`中心の専用スタイル。
- `index.html`：ゲームJS・Arcade CSSのキャッシュ参照更新。
- `README.md`：本書への案内。
- `CHANGES-2048.md`：タイトル、実装内容、保存仕様、制限、PR保存先。

コードの読み取りとGit/PRの保存状態の確認だけを行い、テストファイルの追加・変更やテスト実行は行っていません。スマートフォンの実操作、画面幅別の見た目、スクリーンリーダー、保存容量不足、ブラウザごとのモーション、既存ゲームへの回帰は未検証です。

PRへの保存と本番公開は別です。この作業ではPRをマージせず、本番へのデプロイも行いません。公開後の入口は「ゲーム → 2048」、または `index.html#app=games&view=2048` です。
