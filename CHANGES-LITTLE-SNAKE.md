# リトルスネーク完全版：高精細な庭・なめらかな移動・遊び続けられる機能

## 改修タイトルと保存先

**この改修のタイトル：** `feat(snake): リトルスネークを高精細化し、なめらかな移動・3モード・中断保存・実績を追加`

**共有PRの更新タイトル：** `feat: リトルスネークの高精細化・なめらかモーションとゲーム／操作UIの強化`

- PR：<https://github.com/sc8z35a-collab/smaho-gpt6/pull/11>
- 開発ブランチ：`genspark_ai_developer` → `main`
- 起動：「ゲーム」→「Little Snake」、または `index.html#app=games&view=snake`
- 同じ共有PRにあるコントロールデスク、2048、Crystal Field、Reversi、Orbit Breakerなどの並行変更を保持しています。本書はLittle Snakeの変更説明です。
- **依頼により、Little Snakeのテストは一切実行していません。** 自動テスト、手動プレイ、ブラウザ表示確認、JavaScript構文チェック、性能測定は未実施です。PRの過去の別機能の検証結果は、この改修の動作保証ではありません。
- 「完全版」は今回依頼された機能・説明を揃えたPR保存版の名称であり、不具合がないことを保証する表現ではありません。PR保存と本番マージ・公開は別です。

## 1. グラフィックと滑らかな動き

- つながった丸い胴体、陰影、ハイライト、模様、立体的な頭、瞬きする目へ変更。
- 木もれび／月あかりの2テーマ。深い色のグラデーション、草、小石、光の粒、周辺の陰影、素材感のある盤面フレームを追加。
- 果実に葉・反射・影・発光。金の実には残り時間のリングを表示。
- 果実を取ると粒子と加点数を表示。スコアも小さく拡大して反応します。
- マス単位のゲーム判定と`requestAnimationFrame`の描画を分離。前後の位置と頭の角度を補間します。
- おさんぽモードの壁抜けも補間。盤面を横切る不自然な長い線にならないよう描画を分割します。
- Canvasは表示幅・画素比に追従。高精細は画素比最大2・内部解像度最大1296四方。軽量は画素比1を基本に解像度と装飾を抑えます。

### 負荷とアクセシビリティ

- 背景を別Canvasにキャッシュ。見た目やサイズが変わった場合だけ作り直します。
- 胴体の`Path2D`をフレーム内で再利用。壁抜けの複製描画は近づいた辺だけに絞ります。
- 粒子は最大56個、得点表示は最大8個。演出が無制限に増えないようにしています。
- 停止中は描画ループを止め、画面移動時はイベント・監視・描画資源を解放。
- OSまたはauraの「動きを減らす」で、座標・回転の補間、瞬き、揺れ、粒子、得点アニメーションを抑制。ゲーム進行と残り時間は継続。
- ダークモード、狭い画面、キーボードフォーカス、日本語の操作ラベル、状態通知に対応する実装を追加。
- 特定のFPSや実測の高速化率は保証しません。性能は未測定です。
- Canvasの盤面を音声だけで同等に遊べる実装ではありません。

## 2. 3つのモード

| モード | ルール | 初期間隔 | 最速 |
|---|---|---:|---:|
| クラシック | 壁と体を避け、実を集めるほど加速 | 150ms/マス | 95ms/マス |
| おさんぽ | 壁から反対側へ移動。体への衝突は終了。一定の低速 | 210ms/マス | 210ms/マス |
| ラッシュ | 速い移動と連続獲得コンボ。壁・体への衝突で終了 | 120ms/マス | 70ms/マス |

- 通常の実は10点、金の実は30点。どちらも体が1マス伸びます。
- 実を5個取るごとにレベルが上がり、おさんぽ以外は間隔を6ms短縮。速度上限があります。
- 5個ごとに、空きマスがあり金の実がまだ出ていなければ、金の実が8秒間出現します。
- ラッシュでは6秒以内に次の実を取ると1→2→3→4倍。通常・金の実の両方に適用。期限後は1倍から再開。
- 324マスを埋めるとクリア。満杯の盤面で果実を無限に抽選しません。
- 成長目標は実を5・15・30・60・120・320個集める段階式。進捗バーを表示。
- モード別最高点と自己ベスト更新表示、直近8プレイの履歴、終了回数・累計獲得数・最長の記録を追加。

## 3. 6つの実績

| 実績 | 条件 |
|---|---|
| はじめの一粒 | 実を1個取る |
| 金色の寄り道 | 金の実を取る |
| 庭の探検家 | 長さ24マス |
| リズムマスター | ラッシュで4倍コンボ |
| 豊かな収穫 | 1プレイ500点 |
| 庭の守り手 | 324マスを埋めてクリア |

達成はその場で表示し、端末内に保存。新しいゲームにしても実績は残ります。古い最高点から過去の実績を推測して付与することはしません。

## 4. 操作・一時停止

- 矢印キー、WASD、スワイプ、方向ボタンで操作。
- スワイプと方向ボタンは、指を離す前に入力を受け付けます。
- 方向は2回先まで予約でき、予約方向を基準に逆走を禁止。長押しの連打や同じ方向の重複を抑制。
- Spaceで開始・停止・再開。ボタンや折りたたみ見出しを選択中のSpaceは、その要素の通常操作です。
- タブ移動、ウィンドウのフォーカス喪失、アプリ移動、メニュー表示、見た目と記録の展開時に停止。自動再開はしません。
- フレーム間隔が300msを超えると停止し、遅れた分の移動を一気に処理しません。
- 途中のやり直し・モード変更は確認後に実行。最高点・実績・過去の履歴は保持。

## 5. 途中保存と再開

- プレイ中は2秒ごと・果実取得時、一時停止・画面移動時にも保存します。
- 再読み込み後は、体・方向・果実・得点・獲得数・コンボ・ゲーム内時間などを復元。必ず停止状態で始まり、「再開」で続けます。
- 閉じていた時間は経過時間や金の実・コンボの期限に加算しません。
- 途中保存は全モード共通で1枠。新規ゲーム確定・ゲーム終了時は途中保存を`null`にします。
- 入力予約と描画補間の途中状態は保存しません。復元は確定したマスから行います。
- 復元時に、モード、バージョン、座標、体の重複・連続性、方向、果実との重なり、数値範囲を検査。不正な保存は利用せず、新規の庭を開始します。
- 保存失敗は「未保存」と表示。「今すぐ保存」や次の開始・停止で再試行できます。失敗後の周期的な再試行は止めます。
- OSによる強制終了などでは終了時保存を保証しません。最後に保存できた時点まで戻ることがあります。
- 複数タブの競合解決はありません。同じゲームは1タブで遊ぶことを推奨します。

### 保存キー

すべてブラウザのlocalStorageです。外部通信、クラウド同期、アカウントは追加していません。

| キー | 内容 |
|---|---|
| `aura.snakeBest` | ゲーム一覧の総合最高点。旧記録を保持し、上回ったときだけ更新 |
| `aura.snakeRecords` | モード別最高点、終了回数、終了したプレイの累計獲得数と最長 |
| `aura.snakeHistory` | 終了した最近8プレイのモード・点数・獲得数・秒数・クリア有無 |
| `aura.snakePreferences` | 選択モード、庭、描画品質、マス目 |
| `aura.snakeSession` | バージョン1の途中保存、または`null` |
| `aura.snakeAchievements` | 達成した実績ID |

旧最高点はモード別に自動転記しません。途中で破棄したプレイは終了回数・履歴に加算しません。盤面・記録・履歴・実績は共通の`saveBatch`で保存しますが、localStorageにトランザクションはなく、失敗時の巻き戻しはベストエフォートです。見た目の設定は別に保存します。ブラウザデータ削除で記録は失われます。

## 6. 変更ファイル

- `js/games.js`：Snakeの描画、ルール、入力、中断保存、実績、記録、ヘルプ。
- `css/arcade.css`：Snake専用の盤面・素材・スコア・方向キー・目標・実績・保存状態のスタイル。
- `index.html`：ゲームとスタイルのキャッシュ更新。並行開発の別ゲームの更新も保持。
- `README.md`：本書へのリンク。
- `CHANGES-LITTLE-SNAKE.md`：タイトル、仕様、保存範囲、制約、テスト未実施の記録。

共有ブランチのリモート変更を取り込み、別の改修を消さないよう統合しています。共有履歴を強制上書きしてまとめ直すことはせず、各変更をコミット・pushします。

## 7. テスト・公開状況

**利用者の指定により、今回のSnake変更のテストは未実施です。**

自動テスト、既存回帰テスト、構文チェック、手動プレイ、ブラウザ表示、タッチ、高DPI、バックグラウンド復帰、性能測定は行っていません。実装ソースとGit／PRの保存状態を確認することと、動作テストは区別しています。

このPRを本番へマージ・デプロイする操作は行っていません。
