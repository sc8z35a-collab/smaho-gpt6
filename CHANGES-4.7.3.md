# auraOS 4.7.3 — 総点検バグ修正

全JS（約1MB）の静的レビュー、全30アプリの自動操作ファズ（390px・320px／ライト・ダーク、クリック・キーボード・フォーム送信）、8ゲームの操作ファズ、個別シナリオの再現で見つけた不具合を修正しました。保存データの形式は変更していません（既存データはそのまま読めます）。

## 修正したバグ（41件）

| # | 領域 | 症状 | 修正 |
|---|---|---|---|
| 1 | ロック画面 | Escキーでロック画面が解除される | ロック中はEscで解除しない |
| 2 | ロック画面 | Hキーでロック画面からホームへ抜けられる | ロック中はHを無効化 |
| 3 | ロック画面 | ロック中にAlt+Tabで最近のアプリが開く | ロック中は無効化 |
| 4 | キーボード | document宛てのkeydownで `e.target.closest` 例外 | Element判定を追加（3か所） |
| 5 | 検索 | 本文のないメモで検索すると例外 | 欠損フィールドを空文字として扱う |
| 6 | 検索 | テキストのないリマインダーで検索すると例外 | 同上 |
| 7 | 検索 | 「undefined」で検索すると電話・メール未入力の連絡先が全件ヒット | 未入力値を連結しない |
| 8 | 検索 | 読書の検索結果が編集フォームを開く | 本の詳細を開く |
| 9 | 検索 | 全角・半角の違いで一致しない | NFKC正規化 |
| 10 | 保存 | `saveBatch` が `A.save` の失敗を無視し、関連データの一部だけ保存される | 全書き込みを `A.save` 経由にし、失敗時は巻き戻し |
| 11 | アイコン | `eye`（メモの集中表示・設定）が未定義でグリッドに化ける | 追加 |
| 12 | アイコン | `chevronRight`（タイマー履歴・音源一覧）が未定義 | 追加 |
| 13 | アラーム | 削除済みアラームの操作で例外 | 存在確認 |
| 14 | アラーム | 保存失敗でもON/OFF・追加・削除が画面に反映される | 保存成功時のみ反映 |
| 15 | アラーム | 同じ時刻・ラベルを重複登録できる | 重複を拒否、時刻順に並べる |
| 16 | アラーム | 不正な時刻文字列がそのままHTMLに出力される | 読込時検証とエスケープ |
| 17 | アラーム | 全削除後に空表示がない | 空状態を表示 |
| 18 | ウォレット | 保存失敗でも残高が増減して見える | 保存成功時のみ反映、履歴上限100件 |
| 19 | ウォレット | 壊れた保存データで描画が失敗 | 読込時に検証 |
| 20 | 計算機 | `50 + 10 %` が 50.1 になる | 一般的な電卓どおり 55（− も同様） |
| 21 | 計算機 | 演算子直後の ± が前の数値を反転する | 次の数値を負数で入力開始 |
| 22 | 計算機 | エラー表示中の ±・% で表示が壊れる | 無視 |
| 23 | 設定 | 空白だけの表示名を保存できる | 前後空白を除き、空は拒否 |
| 24 | 設定 | 「キーボード」検索で操作ガイドも重複ヒット | 説明文を修正 |
| 25 | カレンダー | 開始と同じ終了時刻を保存できる | 拒否（開始より前の終了は翌日扱い） |
| 26 | カレンダー | 日をまたぐ予定の終了が当日に見える | 「翌」を表示 |
| 27 | カレンダー | 繰り返し回数の範囲外値が黙って丸められる | 1〜52を検証 |
| 28 | カレンダー | 日付・時刻の形式を検証しない | 検証を追加 |
| 29 | カレンダー（旧版） | 削除の保存失敗でも一覧から消える | 保存成功時のみ反映 |
| 30 | 家計簿 | 予算超過時に「¥-500 残り」と表示 | 「¥500 超過」を警告色で表示 |
| 31 | 写真 | 320px幅でグリッドが画面外にはみ出す | 実際の余白に合わせて全幅表示 |
| 32 | 写真 | 大きな写真ボタンに名前がない（読み上げ不可） | aria-labelを追加 |
| 33 | フォーム | ラベルと入力欄が関連付けられていない（メール・アラーム等） | 自動関連付け |
| 34 | メール（デモ） | 保存失敗でも送信済み・下書きに追加される | 保存成功時のみ反映 |
| 35 | メール（デモ） | 削除の保存失敗でも消える | 同上 |
| 36 | メール（デモ） | 宛先未入力でも「デモ送信」できる | 入力を検証 |
| 37 | メール（デモ） | 検索が大文字小文字を区別し、宛先も対象外 | 大小無視・宛先も検索 |
| 38 | メール（デモ） | 時刻が「今」のまま固定 | 保存時刻を表示 |
| 39 | メッセージ（デモ） | 検索が大文字小文字を区別 | 大小無視 |
| 40 | リマインダー | 不正な保存値で既定のコンパクト表示が解除される | 既定値にフォールバック |
| 41 | キャッシュ | 更新後も古いJS/CSSが使われる | 変更ファイルのキャッシュキーを 4.7.3 に更新 |

## テスト結果

- 追加 `tests/bugfix.cjs`（21シナリオ）：**21 / 21 成功**
- `tests/debug.cjs`：**45 / 45**（修正前は41/45。B03・B04は #10 で解消、B05・B26は現行UIのセレクターに更新）
- `tests/persistence.cjs`：**28 / 28**（リマインダーのコンパクト表示の既定 #40 に追従）
- `tests/settings.cjs`：**12 / 12**（#24 で重複ヒットを解消）
- `tests/index.html`（regression.js）：**158 / 158**（main時点で失敗していた録音・ヘルスケアの4件を、記録形式v2に合わせて更新）
- ファズ：全30アプリ × 390px / 320px で未捕捉例外 0件。8ゲームは矢印キー・ドラッグ・連打で例外 0件。

実機（iOS / Android）での確認は未実施です。
